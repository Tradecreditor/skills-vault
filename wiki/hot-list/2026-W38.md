---
week: 2026-W38
window: "2026-09-14T00:00:00Z .. 2026-09-20T23:59:00Z"
generated_at: "2026-09-20T22:15:03Z"
sources_used: [github_connector, github_api_via_exa, exa_web_search, exa_web_fetch, fxtwitter, skillsmp, clawhub]
quiet_week: true
---

This is the vault's **first** weekly-hot-list run — `_snapshot.json` was empty, so no `stars_gained_7d`
could be computed against a prior week for already-known repos. Every GitHub number below is either a
"new repo" total-stars figure (age ≤ 90 days) or, for `fast-jev-compaction`, a since-creation figure that
stands in for a 7-day gain because the repo is 3 days old. `quiet_week: true` because only **one** candidate
cleared gates on 2+ platforms this week — per `_config.yaml` ("require gates on 2+ platforms OR heat ≥
single_source_min_heat"; under the configured weights, 0.60 is not reachable on a single platform, so in
practice a winner needs two). Thresholds were **not** lowered to force a second or third winner.

## 本週亮點 (winners)

### 1. tamaratran/fast-jev-compaction — heat 0.94 — gates cleared: GitHub + X

Claude Code function-hook plugin (and standalone npm library) that replaces Claude Code's built-in,
summarization-based `/compact` with verbatim, per-tool-call keep/drop decisions scored by TypeSafe's "Jev"
model. Nothing gets paraphrased — a call and its result are either kept exactly as they were, truncated to
a head snippet, or dropped entirely, so an exact file path or error string can't silently vanish inside a
summary the way it can with Claude's default compaction.

- **GitHub** (github new-repo "topical" gain gate, threshold 1,500 stars gained): repo created
  2026-09-17T05:57:20Z (3 days before capture) with **5,089 stars**, 281 forks, 56 open issues — since the
  repo didn't exist before this window, its total stars *are* its 7-day gain. Component = min(1, 5089/1500) = 1.
  Source: `mcp__github__search_repositories` (GitHub API), re-verified via direct repo fetch.
- **X** (gates: likes ≥ 2,000, reposts ≥ 500, views ≥ 1,000,000): creator [@tamarajtran](https://x.com/tamarajtran/status/2100694549362553153)'s
  launch post — **10,686 likes · 657 retweets · 3,693,140 views** (fxtwitter-verified 2026-09-20). Component = 1
  on every sub-metric.
- multi_platform_bonus ×1.25 applied (2 platforms cleared) → heat = (0.40×1 + 0.35×1) × 1.25 = **0.9375**.
- This is the one winner that satisfies the config's "at least one winner must have cleared the X or Threads
  gate" rule.
- Captured: `raw/20260920-fast-jev-compaction.md`, `wiki/pages/20260920-fast-jev-compaction.md`.
- No `skills/<name>/SKILL.md` drafted — this is a Claude Code function-hook **plugin** distributed via
  `claude plugin marketplace add`, not an Agent-Skills-spec `SKILL.md` folder, so it doesn't fit the vault's
  skill format.

*(Only one winner qualified — see quiet-week note above. `winners: {min: 2, max: 3}` normally applies, but
the config is explicit that thresholds are never lowered to hit the minimum.)*

## 候選總表 (all candidates)

| Item | GitHub (stars / age) | GitHub gate | X (likes/RT/views) | X gate | Threads | Registries | Heat | Note |
|---|---|---|---|---|---|---|---|---|
| tamaratran/fast-jev-compaction | 5,089 / 3d | ✅ topical-gain (≥1,500) | 10,686 / 657 / 3.69M (@tamarajtran) | ✅ all 3 | — | not indexed on SkillsMP | **0.94** | **Winner** |
| cloudflare/security-audit-skill | 17,934 / 88d | ✅ new-repo (≥10k, ≤90d) | none found | ❌ | none found | SkillsMP 8,812★ (unofficial mirror) | 0.40 | GitHub Trending #1 three days running, Hacker News 203 pts — but no X/Threads post found despite 3 targeted searches |
| lidge-jun/opencodex | 15,623 / 88d | ✅ new-repo | none found | ❌ | none found | — | 0.40 | Universal LLM proxy for Codex CLI/App/SDK + Claude Code |
| openai/codex-security | 10,814 / 63d | ✅ new-repo | none found | ❌ | none found | — | 0.40 | Official OpenAI CLI/SDK for agentic vuln scanning |
| Thariq Shihipar (@trq212) — Claude Code 2.1.277 AGENTS.md support | n/a (feature, not a new repo) | ❌ n/a | 31,128 / 2,607 / 5.33M | ✅ all 3 | — | — | 0.35 | Biggest X story of the week by far, but ties to no new/trackable repo, so no second gate |
| genspark-ai/genoffice | 7,330 / 51d | ❌ (<10k) | none found | ❌ | none found | — | 0 | — |
| jakubkrehel/skills | 6,938 / 72d | ❌ (<10k) | none found | ❌ | none found | — | 0 | — |

Full raw candidate pool (69 GitHub repos across 10 topic searches + 1 breakout search, all with ≥1,000
stars and created since 2026-06-16) is recorded in `wiki/hot-list/_snapshot.json` (56 entries kept after
dedup) for next week's `stars_gained_7d` deltas. X/Threads searches (9 `web_search_exa` calls + 2 targeted
follow-ups, within the 12-search cap) surfaced no other post with a resolvable `x.com/.../status/<id>` that
fxtwitter could verify — several tools were covered heavily in news/blog roundups but that coverage is
explicitly excluded from this report per the rule "numbers must come from fxtwitter, GitHub or registry
APIs, never search-result snippets."

## Watch (best sub-threshold items)

1. **cloudflare/security-audit-skill** — 17,934 stars, created 2026-06-18 (88 days old, still inside the
   90-day new-repo window). A six-phase, independently-verified security-audit skill for coding agents;
   seeded Cloudflare's internal vulnerability-discovery harness. #1 on GitHub Trending (JS) for three
   consecutive days (Sept 16–18) and 203 points on Hacker News. Single-platform only — watch next week for
   an X/Threads post that would push it over the 2-platform bar.
2. **Thariq Shihipar / Claude Code 2.1.277 — native AGENTS.md support** — Anthropic's Claude Code engineer
   announced (2026-09-18) that Claude Code now reads `AGENTS.md` as a fallback when no `CLAUDE.md` is
   present, closing a public complaint from Shopify's CEO. fxtwitter-verified 31,128 likes / 2,607 retweets
   / 5,327,796 views — the single biggest X number found this week — but it's a product feature inside the
   existing `anthropics/claude-code` repo, not a new/trackable repo, so it can't clear a second gate.
3. **lidge-jun/opencodex** — 15,623 stars, created 2026-06-18. Universal provider proxy letting Codex
   CLI/App/SDK and Claude Code talk to any LLM backend (Claude, Gemini, Grok, DeepSeek, Ollama). Clears the
   GitHub new-repo gate comfortably; no X/Threads corroboration found.

## 註記 (notes / caveats)

- **Queries run** — GitHub: 10 per-topic searches (`created:>=2026-06-16 stars:>=1000`, sorted by stars) +
  1 breakout search (`created:>=2026-09-14 stars:>=1000`), all via `mcp__github__search_repositories`
  (GitHub API; not the `gh` CLI, which is proxy-blocked here). X: `web_search_exa` with `category:tweet`
  across 4 topic/tool-specific queries, plus 2 `site:x.com`/`site:twitter.com` retries (both returned no
  results — Exa's `site:` operator did not work reliably for x.com) and 3 further targeted queries once
  named accounts (@trq212, @tamarajtran) surfaced in news coverage. Threads: 1 `site:threads.net` query,
  no dated/engaged posts from this window found. Total `web_search_exa` calls: 11 of the 12-call cap.
  `fxtwitter_fetches`: 2 of the 40-call cap.
- **Caps hit**: none hard-hit; stopped GitHub-only candidates' X search after 3 dedicated attempts each
  rather than exhaust the Exa cap chasing repos that may simply not have spread on X/Threads this cycle.
- **What failed / was skipped**: `skills.sh`'s trending API requires a Vercel OIDC bearer token this vault
  doesn't have (`authentication_required`) — skipped, noted for `_config.yaml` maintainers. ClawHub's
  trending endpoint works but returned only long-established skills (steipete's CLIs), none of this week's
  candidates. SkillsMP search found `cloudflare/security-audit-skill` mirrored with 8,812★ (an unofficial
  third-party star mirror, not an install/download count, so **not** used as a gate metric — informational
  only). No `TWITTER_AUTH_TOKEN`/`TWITTER_CT0` in the environment, so X used the Exa `category:tweet`
  fallback throughout, as `_config.yaml` specifies.
- **Threads**: no dated, on-topic post from the 2026-09-14..20 window was found with resolvable engagement
  numbers. Per `_config.yaml`, Threads has no official trending feed and is labelled "loud among posts
  found" rather than gated — this week nothing rose above that bar with verifiable numbers.
- **Snapshot**: `_snapshot.json` was empty before this run (vault's first hot-list). Populated with 56
  repos' current `stars`/`as_of` from this week's searches so next week can compute real `stars_gained_7d`
  deltas instead of treating every candidate as "new."
