# Vault backlog fixes — 2026-W39（2026-09-26）

一次過清理 `vault-lint` 連續三次標出但冇人修嘅問題，同 09-21 改咗 access model 之後過時嘅文件／script。
Branch：`claude/wizardly-dijkstra-a21g27`（未 merge 入 `main`；commit 數見 compare link）。
Compare：<https://github.com/Tradecreditor/skills-vault/compare/main...claude/wizardly-dijkstra-a21g27?expand=1>

執行方式跟 `skills/model-tiering`：Sonnet 做執行，主循環（Fable）做拆解同最終審查，每個 executor 嘅 diff 由一個
Fable refuter 嘗試推翻先 commit。Refuter 一共推翻咗 7 批（guard 兩個 bypass、README 對真實 ruleset 嘅講法、
routines/README 嘅 DST 算術同 author 事實、Supabase redeploy 講法、setup-phase1 漏咗安裝步驟、hot-list prompt 引用錯步驟），
全部已修正。最後三個 Fable 審查（規則違反／跨檔矛盾／完整性）再搵出 `model-tiering` description 未加引號（YAML 唔合法）、workflow 註解話 guard 已係 required、README 叫其他 agent 直接 push、資料夾地圖漏咗 templates/ .obsidian/ 等 12 處，都已修；guard 而家會擋未加引號而含 `: ` 嘅 description。`raw/` 一個 byte 都冇郁；`wiki/log.md` 只喺尾加行。

## 摘要

| 範圍 | 之前 | 之後 |
|---|---|---|
| 缺 `author` 嘅 star 筆記 | 8 | 0 |
| `## Related` 空白 | 29 / 38 | 0 / 39 |
| 孤兒頁（冇任何 inbound link 又唔喺 hot.md） | 10 | 0 |
| index tags 同 frontmatter 唔一致 | 8 行 | 0 |
| skill 名含 "claude" / description 超 300 | 1（`stacking-claude-code-plugins`，360 字） | 0（改名 `stacking-coding-agent-plugins`，296 字） |
| guard script 對齊 CLAUDE.md | 唔對齊（1024 字、冇 "Use when"、保護路徑 4 個、非 ASCII 路徑會漏） | 對齊並用 fixture 驗證過（兩個身份 × 9 種情況） |
| `raw/` 規則 | 文件講「非核心唔可以掂」，script 只擋修改 | 統一為「所有人只准新增」（非核心 capture PR 本身要加 raw/） |
| 文件矛盾（private / `tradecreditor-agents` / approvals=0 / Haiku / `~/Vaults`） | 12 處 | 0 |
| Windows / macOS script 對稱 | copy-prompt 只有 .ps1；setup-phase1 步驟數唔同；hook ps1 四處落後 | 三對都一致 |

## 逐項狀態（對應計劃 A 節）

| # | 項目 | 狀態 | Commit |
|---|---|---|---|
| 1 | vault-capture 叫 routine 設假 author `skills-vault-core` | 已修 | f2572c7 |
| 2 | vault-lint 用 `type` 判斷資料夾（同 CLAUDE.md 相反） | 已修，改用 slug 形態 | c3ef58f |
| 3 | stars-sync model 兩處寫 Haiku | 已修為 Sonnet | c3ef58f |
| 4 | 8 個 star 缺 `author` | 已補 | 603bcd2 |
| 5 | hot-list config：skills.sh API 要 token、seed topic 撈唔到贏家、Exa 冇 date 參數 | 已修（+3 topic、Trendshift discovery、snowflake 日期預篩、cap 30） | c3ef58f |
| 6 | `hot-list/2026-W39` 舊 branch 會撞 | prompt 改 `checkout -B` + `--force-with-lease`（只限 hot-list/*） | c3ef58f |
| 7 | guard script 鬆 | 收緊 + 修兩個 bypass（C-quoted 路徑、多行 description） | 870af0d, f511aac |
| 8 | search.sh 永遠唔印 `(none)` | 已修 | 63b4a98 |
| 9 | README 過時（private、plugins/、符號連結、star 數、第 5 步舊計劃） | 已重寫，第 5 步對齊真實 ruleset | 5185db8 |
| 10 | routines/README 兩段 timezone、「main 所有 commit 必須係你」 | 已合併／改為實況 | 460dbf3 |
| 11 | bootstrap `--private`、setup-phase1 講 private | 已改 public | 63b4a98, 5ac4965 |
| 12 | `~/Vaults/skills-vault` 路徑不一致 | 統一 `~/skills-vault` | 63b4a98, 5ac4965 |
| 13 | setup-phase1.ps1 少兩步 | 兩邊 1/6..6/6 一致，並真係安裝 plugin | 5ac4965 |
| 14 | 冇 copy-prompt.sh | 新增，UTF-8 round-trip 核對 | dec6ff2 |
| 15 | vault-capture 四處同 CLAUDE.md 矛盾；skills/README 冇表 | 已修；表列 8 個 skill；lint 第 3 項會核對個表 | 2adc343 |
| 16 | Supabase README 過時；GRANT 加咗入已 apply 嘅 migration | 新 migration `20260925000000_captures_grants.sql`；README 修正 | 417f030, 464f62c |
| 17 | vault-suggest.ps1 落後 .sh | 已對齊（含 20 秒 pull timeout） | dec6ff2 |
| 18 | 29 頁 Related 空白、10 頁孤兒 | 全部填好（每頁 2–5 條 + 繁中理由） | a5998eb, 7ac9134 |
| 19 | fast-jev 缺 `2026-W38` tag；index tags 唔一致 | 已修 | 603bcd2 |
| 20 | obsidian-skills `published` 係 placeholder | 用 GitHub `created_at` 2026-01-02 | c164fe2 |
| 21 | 10 個 star 冇 draft SKILL.md | **你決定唔做**，留俾 stars-sync routine | — |
| 22 | 根目錄三個空檔；daily notes 冇資料夾 | 空檔你已喺 main 刪（1cfe1c0）；daily notes 改落 `inbox/daily/` | 2e89b05 |
| 23 | `wiki/log.md` 6–7 行次序亂 | 刻意唔郁（append-only） | — |
| 24 | 14 頁 `captured_at` 係 `T00:00:00Z` | 刻意唔郁（歷史） | — |
| 25 | `stacking-claude-code-plugins` 名含 claude、description 360 字 | 改名 `stacking-coding-agent-plugins`（核心身份改名，一日大嘅資料夾） | 05ce0dc |
| + | `model-tiering` skill 入 vault | `skills/model-tiering/`（draft） | 4143c4d |

## 要你親手做嘅嘢（repo 外）

1. **Ruleset 加 `guard` 做 required status check**：用 API 睇過 `main-protection` 嘅 `required_status_checks` 係空，即係而家紅叉唔擋 merge。
   個 check 要喺一個 PR 度跑過先揀得到——用 `tradecreditor-ui` 開第一個 PR 之後即刻去 Settings → Rules → `main-protection` 加。
2. **重貼 `capture-link` 嘅 prompt**：live Routine 仍然用舊 prompt（有 `git config user.name "skills-vault-core"`），09-26 嘅 capture 仲係用假 author。
   `scripts/copy-prompt.ps1 capture-link`（或 `.sh`）貼一次。`weekly-hot-list` 同 `vault-lint` 嘅 prompt 都改咗，一樣要重貼。
   注意 capture-link 跑嗰陣會讀 `skills/vault-capture/SKILL.md`，而 `main` 上嗰份仍有假 author 嗰句——所以 merge 呢個 branch 同重貼 prompt 兩樣都要做。
3. **Routine cron**：核對三個定時 Routine 最後一格係 `1`（vault-lint 09-21/22/23 每日跑過）；睇 UI 嘅 next run。
4. **Supabase**：`supabase db push`（跑新 migration）；`supabase secrets set TELEGRAM_ALLOWED_USERS=<你嘅 Telegram ID>`（先 message 個 bot 攞 ID）；iPhone 捷徑 + Telegram 端對端測一次。
5. **驗收測試**（README 第 5 步第 5 點）從未做過：用 `tradecreditor-ui` 開 PR 試加 raw/、改 raw/、刪頁、掂 routines/。
6. **`.obsidian/graph.json`**：仍然 tracked，每次 Obsidian 開關都會入 backup commit。如果想停，喺你本機跑
   `git rm --cached .obsidian/graph.json && echo .obsidian/graph.json >> .gitignore` 再 commit——喺遠端做會令你部機 obsidian-git 出 modify/delete conflict，所以我冇做。
7. **舊 branch**：`origin/hot-list/2026-W39`（PR #4 已關）同 15 條 `claude/*` session branch 可以刪。
8. **PAT**：`tradecreditor-ui` 嘅 classic PAT 約 2026-12-20 到期。
9. **Review draft**：39 篇筆記全部 `status: draft`，6 個 skill `vault_status: draft`（含 `model-tiering`，你講一聲我改 verified）。10-13 起 lint 會標 30 日以上嘅 draft。
10. **Merge 呢個 branch**：guard 收緊咗嘅規則要 merge 入 main 之後先對 PR 生效（workflow 跑 base branch 嗰份 script）。

## 驗證（最後一次跑，全綠）

```
bash scripts/vault-guard-check.sh origin/main HEAD Tradecreditor      # all checks passed
bash scripts/vault-guard-check.sh origin/main HEAD tradecreditor-ui   # 只因保護路徑 + 改名紅叉（符合預期）
diff CLAUDE.md AGENTS.md                                              # identical
bash -n scripts/*.sh .claude/hooks/*.sh skills/*/scripts/*.sh          # ok
python3 -m json.tool templates/*.json .claude-plugin/*.json .obsidian/*.json   # ok
yaml.safe_load(wiki/hot-list/_config.yaml)                            # ok
39 篇筆記：15 個 frontmatter key 齊、captured_at 嚴格 ISO-Z、index 雙向一致、摘要 ≥20 個中文字
8 個 skill：name==folder、description ≤300 含 "Use when"、body <500
git diff --stat origin/main -- raw/                                   # 空
git diff origin/main -- wiki/log.md | grep '^-'                       # 空（append-only）
```
