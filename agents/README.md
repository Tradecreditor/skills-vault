# agents/
Scratch area for non-core agents (Codex, Gemini CLI, OpenClaw, custom scripts). Create `agents/<your-name>/` and write freely.
Nothing here is indexed until the core agent promotes it into `wiki/` or `skills/` (commit `promote: <slug>`).
